# runtime data: frames, thumbs, SQLite (--data-dir default)
/data/

# venvs: main program uses --system-site-packages, sidecar has its own
.venv/
sidecar/.venv/

# upstream clone (its own git repo) plus the HF model cache it holds -- ~30GB
/vLLM/jetson-containers/

*.egg-info/
__pycache__/
*.py[cod]

# the telegram bot token, read by bot/telegram_bot.py at startup
.env

# process logs written by ./start.sh
/run/
